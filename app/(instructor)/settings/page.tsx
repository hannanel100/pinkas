/**
 * Settings. SDD §6.1 — holds default_buffer_days, which seeds every new course (§7.2).
 *
 * Scaffold placeholder — owned by `frontend`. Renders no product copy on
 * purpose: Hebrew strings belong in the translation layer (SDD §11), which is
 * ticketed. This file exists so the route, the RTL shell and the JS budget
 * check are real from the first commit.
 */
export default function SettingsPage() {
  return <main className="p-4">Settings</main>;
}
